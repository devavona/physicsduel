# Release builds are unsigned/debug-only for this project (no Play Store distribution),
# and minifyEnabled is currently false, so nothing here is exercised yet. Kept as a
# landing spot for LibGDX/reflection-based keep rules if minification is ever turned on.
